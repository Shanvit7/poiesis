# No runtime env vars needed for the web app.
#
# The only build-time variable is NEXT_PUBLIC_BASE_PATH, which is set
# in the GitHub Actions deploy workflow (not in .env) when deploying to
# GitHub Pages. For local development it defaults to "" (empty string).
#
# See: .github/workflows/deploy.yml
